name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
env:
  UV_CACHE_DIR: /tmp/.uv-cache

jobs:
  test:
    env:
      UV_CACHE_DIR: /tmp/.uv-cache
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: [ '3.10', '3.11']
        include:
          - os: ubuntu-latest
            uv_cache_path: /tmp/.uv-cache
            UV_CACHE_DIR: /tmp/.uv-cache
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Tell me where the UV_CACHE_DIR is
        run: |
          echo "UV cache dir is $UV_CACHE_DIR"
          echo "UV cache dir is ${{ env.UV_CACHE_DIR }}"
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.8.22"
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Create cache directory if not exists
        run: mkdir -p /tmp/.uv-cache

      - name: Restore uv cache
        uses: actions/cache@v4
        with:
          path: /tmp/.uv-cache
          key: uv-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: | # fallback key
            uv-${{ runner.os }}-

      - name: Install package and check import
        run: |
          uv venv
          source .venv/bin/activate
          uv pip install -e .
          python -c "import amica"

      - name: Install dev extras
        run: |
          source .venv/bin/activate
          uv pip install -e ".[dev]"

      - name: Run tests
        run: |
          source .venv/bin/activate
          pytest src/amica/tests/test_amica.py
      
      - name: Minimize uv cache
        run: uv cache prune --ci